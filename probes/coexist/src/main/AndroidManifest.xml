<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No uses-native-library here on purpose: the probe records what the AAR manifests merge in. -->
    <application android:label="hfmodels coexist probe" android:allowBackup="false">
        <activity android:name=".ProbeActivity" android:exported="true" />
    </application>
</manifest>
