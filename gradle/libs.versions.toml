# Dependency pins for the SDK modules and the probes. The runtime versions here must match
# gradle.properties (litertlmVersion / litertVersion / coroutinesVersion), which the build
# scripts read so a -P override re-gates every module together.
[versions]
androidx-test-runner = "1.7.0"
androidx-test-ext-junit = "1.3.0"
junit = "4.13.2"

[libraries]
androidx-test-runner = { module = "androidx.test:runner", version.ref = "androidx-test-runner" }
androidx-test-ext-junit = { module = "androidx.test.ext:junit", version.ref = "androidx-test-ext-junit" }
junit = { module = "junit:junit", version.ref = "junit" }
