<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- RECORD_AUDIO: the voice-gate screen listens with the platform SpeechRecognizer.
         INTERNET: the first download of the tokenizer / config files (SDK) and of the extraction model's files (GlinerAssets). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 11+: an app must declare the speech recognition service it binds to. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:largeHeap="true">
        <activity android:name=".MainActivity" android:exported="true" android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- The scripted demo for a screen recording; how to start it is in the KDoc of DemoActivity.kt -->
        <activity android:name=".DemoActivity" android:exported="true" android:screenOrientation="portrait" android:theme="@android:style/Theme.Material.NoActionBar" />
    </application>
</manifest>
